# Appfile: App Store Connect / Apple Developer config
# https://docs.fastlane.tools/advanced/Appfile/
#
# Values can be overridden by .env (FASTLANE_USER, FASTLANE_TEAM_ID, etc.)
# or set here. Run `fastlane spaceauth` to store credentials.

for_platform :ios do
  # app_identifier "com.pingak9.scoreboard"  # set per app via input/data.json
  # team_id ""
  # itc_team_id ""
end
